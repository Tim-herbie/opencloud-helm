providers:
  kubernetesGateway:
    enabled: true

service:
  spec:
    type: NodePort

ports:
  web:
    port: 80
    expose:
      default: true
    nodePort: 30080

  websecure:
    port: 443
    expose:
      default: true
    nodePort: 30443