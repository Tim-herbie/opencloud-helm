# OpenLDAP Secrets — placeholder values only, safe for a throwaway test cluster.
# !! CHANGE ME for anything real. These keys are merged into the Helm values
# via valuesFrom on the HelmRelease.
apiVersion: v1
kind: Secret
metadata:
  name: openldap-secrets
  namespace: openldap
  labels:
    app.kubernetes.io/part-of: opencloud
    app.kubernetes.io/component: openldap
type: Opaque
stringData:
  global.adminPassword: admin
  global.configPassword: config