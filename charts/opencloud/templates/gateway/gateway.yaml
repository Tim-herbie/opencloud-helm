{{- if .Values.httpRoute.gateway.create }}
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.httpRoute.gateway.name }}
  namespace: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
  labels:
    {{- include "opencloud.labels" . | nindent 4 }}
  {{- with .Values.httpRoute.gateway.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gatewayClassName: {{ .Values.httpRoute.gateway.className }}
  {{- with .Values.httpRoute.gateway.infrastructure }}
  infrastructure:
      {{- toYaml . | nindent 4 }}
  {{- end }}
  listeners:
    - name: {{ .Values.httpRoute.gateway.sectionName }}
      {{- if .Values.global.tls.enabled }}
      protocol: HTTPS
      {{- else }}
      protocol: HTTP
      {{- end }}
      port: {{ .Values.httpRoute.gateway.port }}
      hostname: {{ .Values.global.domain.opencloud | quote }}
      {{- if .Values.global.tls.enabled }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ .Values.global.tls.secretName }}
            namespace: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
      {{- end }}
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
    {{- if .Values.oidc.enabled }}
    - name: {{ .Values.httpRoute.gateway.sectionName }}
      {{- if .Values.global.tls.enabled }}
      protocol: HTTPS
      {{- else }}
      protocol: HTTP
      {{- end }}
      port: {{ .Values.httpRoute.gateway.port }}
      hostname: {{ .Values.global.domain.oidc | quote }}
      {{- if .Values.global.tls.enabled }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ .Values.global.tls.secretName }}
            namespace: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
      {{- end }}
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
    {{- end }}
    {{- if and (eq .Values.opencloud.storage.mode "s3") .Values.opencloud.storage.s3.enabled .Values.opencloud.storage.s3.httpRoute.enabled }}
    - name: {{ .Values.httpRoute.gateway.sectionName }}
      {{- if .Values.global.tls.enabled }}
      protocol: HTTPS
      {{- else }}
      protocol: HTTP
      {{- end }}
      port: {{ .Values.httpRoute.gateway.port }}
      hostname: {{ .Values.global.domain.minio | quote }}
      {{- if .Values.global.tls.enabled }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ .Values.global.tls.secretName }}
            namespace: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
      {{- end }}
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
    {{- end }}
    {{- if .Values.collabora.enabled }}
    - name: {{ .Values.httpRoute.gateway.sectionName }}
      {{- if .Values.global.tls.enabled }}
      protocol: HTTPS
      {{- else }}
      protocol: HTTP
      {{- end }}
      port: {{ .Values.httpRoute.gateway.port }}
      hostname: {{ .Values.global.domain.collabora | quote }}
      {{- if .Values.global.tls.enabled }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ .Values.global.tls.secretName }}
            namespace: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
      {{- end }}
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
    {{- end }}
    {{- if .Values.collaboration.enabled }}
    {{- if .Values.global.tls.enabled }}
    - name: collaboration-https
    {{- else }}
    - name: collaboration-http
    {{- end }}
      {{- if .Values.global.tls.enabled }}
      protocol: HTTPS
      {{- else }}
      protocol: HTTP
      {{- end }}
      port: {{ .Values.httpRoute.gateway.port }}
      hostname: {{ .Values.global.domain.wopi | quote }}
      {{- if .Values.global.tls.enabled }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ .Values.global.tls.secretName }}
            namespace: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
      {{- end }}
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.httpRoute.gateway.namespace | default .Release.Namespace }}
    {{- end }}
{{- end }}
